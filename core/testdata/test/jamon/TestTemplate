The very top!
<%import>
  java.math.BigDecimal
</%import>

<%args>
  BigDecimal amount
  int x => 3
  int y => 7
</%args>

Here is a template. It has two arguments:

x, which is <% x %>,
and amount, which is <% amount %>.
%  for (int i = 0; i < y; ++i)
% {
i is <% i %>.
<%java> } </%java>
